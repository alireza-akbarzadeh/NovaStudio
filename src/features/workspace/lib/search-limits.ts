/** Max text-search matches returned per page (worker + UI). */
export const SEARCH_MATCH_PAGE_SIZE = 200;

/** Max file-name search results shown per page. */
export const SEARCH_FILE_NAME_LIMIT = 200;
