import type { ReactNode } from "react";

/** Marketing route group for contact & waitlist (URL unaffected). */
export default function MarketingRouteGroupLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}
